# yaml-language-server: $schema=https://raw.githubusercontent.com/eksctl-io/eksctl/main/pkg/apis/eksctl.io/v1alpha5/assets/schema.json
# Comentario para ajustar schema e os plugins nao ficar enchendo o saco.
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: semana-devops
  region: us-east-1
  version: "1.31"
managedNodeGroups:
  - name: workers
    instanceType: t3.micro
    desiredCapacity: 2
    minSize: 1
    maxSize: 4
    volumeSize: 20